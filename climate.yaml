  - platform: generic_thermostat
    name: Küche
    heater: switch.heizung_kuche
    target_sensor: sensor.klima_kuche_temperature
    min_temp: 10
    max_temp: 30
    ac_mode: false
    target_temp: 17
    cold_tolerance: 0.3
    hot_tolerance: 0
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "off"
    away_temp: 16
    precision: 0.1
    
  - platform: generic_thermostat
    name: Wohnzimmer
    heater: switch.heizung_wohnzimmer
    target_sensor: sensor.klima_wohnzimmer_temperature
    min_temp: 10
    max_temp: 30
    ac_mode: false
    target_temp: 17
    cold_tolerance: 0.3
    hot_tolerance: 0
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "off"
    away_temp: 16
    precision: 0.1
    
  - platform: generic_thermostat
    name: Bad unten
    heater: switch.heizung_bad
    target_sensor: sensor.klima_bad_unten_temperature
    min_temp: 10
    max_temp: 30
    ac_mode: false
    target_temp: 17
    cold_tolerance: 0.3
    hot_tolerance: 0
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "off"
    away_temp: 16
    precision: 0.1